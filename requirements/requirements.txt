# This file mirrors requirements/requirements.in.
# A full `uv pip compile` lock could not be regenerated for the June 2026
# refresh because the forward-looking pins (langchain>=1.0, langgraph>=1.1,
# langchain-community>=1.0) are not yet resolvable against the current index.
# Recompile with `uv pip compile ./requirements/requirements.in -o ./requirements/requirements.txt`
# once the 1.x releases are published.
ipykernel
openai>=1.60
langchain>=1.0
langgraph>=1.1
langsmith
langchain-cli
langserve
langchain-openai>=1.0
langchain-ollama
langchain-community>=1.0
langchain-pinecone
langchainhub
langchain-chroma
pydantic
instructor
pypdf
chromadb
arxiv
xmltodict
duckduckgo-search
playwright
matplotlib
streamlit
python-dotenv
unstructured
pysqlite3
wget
plotly
